# 🌟 Bright Star — Run Memory

This file is auto-generated by Bright Agent. It records what the agent learned while building, authenticating, and scanning this application so future runs can skip rediscovery. Safe to commit. Delete it to force a full fresh discovery.

_Generated: 2026-07-09T13:50:52.655Z_

## Tech Stack

- **Languages:** JavaScript, TypeScript
- **Frameworks:** NestJS
- **Databases:** PostgreSQL

## Startup

- **Command:** `docker compose -f compose.local.yml up -d --build --force-recreate`
- **Port:** 3000
- **Health check path:** `/api/config`

## First-Run Setup

- **Completed:** yes

## Authentication

- **Has auth:** yes
- **Auth object ID:** `5dABcYLxCAxMKKCQjX32zm`
- _hint:_ [scan-prep-warning] Scan-prep rejected: model claimed completion but made only 1/5 required rapid POST login probes (need ≥5 to prove rate-limits are relaxed) — re-running
- _hint:_ [auth-detection] jwt auth uses POST /api/auth/login with json body {"user":"admin","password":"admin","op":"basic"}. Token location=header, field/header=authorization, request header=authorization, prefix="".
- _hint:_ [auth-extracted-credentials] UNVERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {"user":"admin","password":"admin","op":"basic"} email=admin, password=admin The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/json"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ Host tool probe_url could not connect to http://localhost:3000, but docker ps shows the app container brokencrystals_t-nodejs-1 exposes 3000/tcp without a published host port. The accessible host-mapped port for the app appears unavailable; localhost:8080 is Keycloak admin redirect and not the target app.
- _hint:_ [auth-detection] jwt auth uses POST /api/auth/login with json body {"user":"admin","password":"admin","op":"basic"}. Token location=header, field/header=authorization, request header=Authorization, prefix="Bearer ".
- _hint:_ [auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {"user":"admin","password":"admin","op":"basic"} email=admin, password=admin The login step in the auth object MUST include headers: [{name: "Content-Type", value: "application/json"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.
- _hint:_ [auth-test-failure] loginUrl=http://localhost:3000/api/auth/login, testUrl=http://localhost:3000/api/testimonials, authStyle=jwt, reauthStrategy=status, csrfUrl=none failed: stage=validation status=failure — The response did not match any auth triggers (HTTP 200, application/json; charset=utf-8, body=[]…)

## Hints

**startup**
- Use `docker compose -f compose.local.yml up -d --build` to start the app stack. The backend container is service `nodejs`, published on host port 3000 and serves `http://127.0.0.1:3000/`. The stack also brings up db, keycloak-db, keycloak, mailcatcher, ollama, and grpcwebproxy.

---

<!-- BRIGHT_STAR_DATA — do not edit by hand; regenerated each run -->
```json
{
  "version": 1,
  "generatedAt": "2026-07-09T13:50:52.655Z",
  "techStack": {
    "languages": [
      "JavaScript",
      "TypeScript"
    ],
    "frameworks": [
      "NestJS"
    ],
    "databases": [
      "PostgreSQL"
    ]
  },
  "startup": {
    "command": "docker compose -f compose.local.yml up -d --build --force-recreate",
    "port": 3000,
    "prerequisites": [],
    "envVars": {},
    "healthCheckPath": "/api/config"
  },
  "setup": {
    "completed": true
  },
  "auth": {
    "hasAuth": true,
    "authObjectId": "5dABcYLxCAxMKKCQjX32zm",
    "hints": [
      "[scan-prep-warning] Scan-prep rejected: model claimed completion but made only 1/5 required rapid POST login probes (need ≥5 to prove rate-limits are relaxed) — re-running",
      "[auth-detection] jwt auth uses POST /api/auth/login with json body {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}. Token location=header, field/header=authorization, request header=authorization, prefix=\"\".",
      "[auth-extracted-credentials] UNVERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"} email=admin, password=admin The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/json\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "Host tool probe_url could not connect to http://localhost:3000, but docker ps shows the app container brokencrystals_t-nodejs-1 exposes 3000/tcp without a published host port. The accessible host-mapped port for the app appears unavailable; localhost:8080 is Keycloak admin redirect and not the target app.",
      "[auth-detection] jwt auth uses POST /api/auth/login with json body {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}. Token location=header, field/header=authorization, request header=Authorization, prefix=\"Bearer \".",
      "[auth-extracted-credentials] VERIFIED seeded credentials — use these EXACTLY: POST /api/auth/login Content-Type: application/json Body: {\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"} email=admin, password=admin The login step in the auth object MUST include headers: [{name: \"Content-Type\", value: \"application/json\"}] and the body EXACTLY as shown above. Do NOT use form-urlencoded if the body is JSON.",
      "[auth-test-failure] loginUrl=http://localhost:3000/api/auth/login, testUrl=http://localhost:3000/api/testimonials, authStyle=jwt, reauthStrategy=status, csrfUrl=none failed: stage=validation status=failure — The response did not match any auth triggers (HTTP 200, application/json; charset=utf-8, body=[]…)"
    ],
    "authObjectJson": {
      "otp": null,
      "id": "5dABcYLxCAxMKKCQjX32zm",
      "updatedAt": "2026-07-09T08:14:32.617Z",
      "createdAt": "2026-07-09T08:14:32.617Z",
      "repeaterRequired": true,
      "name": "Engine Auth — jwt",
      "type": "multistep",
      "config": {
        "multistep": {
          "steps": [
            {
              "name": "login",
              "request": {
                "url": "http://localhost:3000/api/auth/login",
                "protocol": "http",
                "method": "POST",
                "headers": [
                  {
                    "name": "Content-Type",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  },
                  {
                    "name": "Accept",
                    "value": "application/json",
                    "type": "clear_text",
                    "mergeStrategy": "replace"
                  }
                ],
                "body": "{\"user\":\"admin\",\"password\":\"admin\",\"op\":\"basic\"}",
                "bodyType": "clear_text"
              },
              "successResponseDetection": [
                {
                  "type": "status",
                  "statuses": [
                    200,
                    201
                  ]
                }
              ]
            }
          ],
          "embedders": [
            {
              "type": "header",
              "name": "Authorization",
              "mergeStrategy": "replace",
              "templateType": "clear_text",
              "template": "Bearer {{ auth_object.stages.login.response.headers | get: '/Authorization' | match:/(?:Bearer\\s+)?([^\\s,;]+)/ }}"
            }
          ]
        }
      },
      "invalidSessionDetection": [],
      "reauthTriggers": [
        {
          "type": "TRIGGER",
          "location": "status",
          "statuses": [
            401,
            403
          ]
        }
      ],
      "projectId": "72Y3LXtc2t5qhhNvEnADxT",
      "successResponseDetection": [
        {
          "type": "status",
          "statuses": [
            200
          ]
        }
      ],
      "description": null,
      "test": {
        "request": {
          "url": "http://localhost:3000/api/users/me",
          "protocol": "http",
          "method": "GET",
          "bodyType": "clear_text"
        },
        "repeaterId": "jjGvr3woLcG5rf74kT6Pkx"
      },
      "staticHeaders": null
    }
  },
  "hints": {
    "startup": [
      "Use `docker compose -f compose.local.yml up -d --build` to start the app stack. The backend container is service `nodejs`, published on host port 3000 and serves `http://127.0.0.1:3000/`. The stack also brings up db, keycloak-db, keycloak, mailcatcher, ollama, and grpcwebproxy."
    ]
  }
}
```
<!-- BRIGHT_STAR_DATA -->
